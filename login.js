const loginForm=document.getElementById('loginForm'),passwordInput=document.getElementById('loginPassword'),message=document.getElementById('loginMessage'),toggle=document.getElementById('togglePassword');toggle.addEventListener('click',()=>{const show=passwordInput.type==='password';passwordInput.type=show?'text':'password';toggle.textContent=show?'Hide':'Show';toggle.setAttribute('aria-label',show?'Hide password':'Show')});loginForm.addEventListener('submit',event=>{event.preventDefault();if(!loginForm.checkValidity()){loginForm.reportValidity();return}const name=document.getElementById('loginEmail').value.split('@')[0];localStorage.setItem('savoure-user',name);message.textContent=`Welcome, ${name}. Taking you to your table…`;message.className='login-message success';setTimeout(()=>window.location.href='index.html',900)});
